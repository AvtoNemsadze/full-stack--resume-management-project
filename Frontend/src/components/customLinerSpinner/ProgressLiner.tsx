import { Box, LinearProgress } from "@mui/material";

const ProgressLiner = () => {
   return (
      <Box sx={{ width: "100%" }}>
         <LinearProgress />
      </Box>
   );
};

export default ProgressLiner;